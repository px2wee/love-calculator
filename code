print("The Love Calculator is calculating your score...")
name1 = input() # What is your name?
name2 = input() # What is their name?
# 🚨 Don't change the code above 👆
# Write your code below this line 👇
name = name1 + name2
digit1 = name.count('t')+name.count('r')+name.count('u')+name.count('e')
digit2 = name.count('l')+name.count('o')+name.count('v')+name.count('e')
score = int(str(digit1) + str(digit2))
if score < 10 or score > 90:
  match = ", you go together like coke and mentos"
elif score > 40 and score < 50:
  match = ", you are alright together"
else:
  match = ""

print(f"Your score is {score}{match}.")
